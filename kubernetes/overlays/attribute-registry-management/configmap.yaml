apiVersion: v1
kind: ConfigMap
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
data:
  POSTGRES_DB: "persistence_management"
  POSTGRES_SCHEMA: "{{NAMESPACE}}_attribute_registry"
  PARTY_REGISTRY_PROXY_URL: "http://{{PARTY_REGISTRY_PROXY_SERVICE_NAME}}:8088/{{PARTY_REGISTRY_PROXY_SERVICE_NAME}}/{{PARTY_REGISTRY_PROXY_INTERFACE_VERSION}}"
