apiVersion: v1
kind: ConfigMap
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
data:
  KAFKA_CLIENT_ID: "{{NAMESPACE}}-pagopa-interop-agreement"
  KAFKA_GROUP_ID: "{{NAMESPACE}}-agreement-readmodel-writer"
  AGREEMENT_TOPIC: "{{ARGEEMENT_TOPIC}}"
