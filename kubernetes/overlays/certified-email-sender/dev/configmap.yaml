apiVersion: v1
kind: ConfigMap
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
data:
  KAFKA_CLIENT_ID: "{{NAMESPACE}}-pagopa-interop-certified-email-sender"
  KAFKA_GROUP_ID: "{{NAMESPACE}}-certified-email-sender"

  PEC_SMTP_ADDRESS: "{{PEC_SMTP_ADDRESS}}"
  PEC_SMTP_PORT: "{{PEC_SMTP_PORT}}"
  PEC_SMTP_SECURE: "{{PEC_SMTP_SECURE}}"
