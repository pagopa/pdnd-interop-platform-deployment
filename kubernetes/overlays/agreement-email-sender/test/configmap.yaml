apiVersion: v1
kind: ConfigMap
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
data:
  KAFKA_CLIENT_ID: "{{NAMESPACE}}-pagopa-interop-agreement-email-sender"
  KAFKA_GROUP_ID: "{{NAMESPACE}}-agreement-email-sender"
  SMTP_ADDRESS: "{{PEC_SMTP_ADDRESS}}"
  SMTP_PORT: "{{PEC_SMTP_PORT}}"