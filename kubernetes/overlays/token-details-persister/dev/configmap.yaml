apiVersion: v1
kind: ConfigMap
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
data:
  KAFKA_CLIENT_ID: "{{NAMESPACE}}-pagopa-interop-token-details-persister"
  KAFKA_GROUP_ID: "{{NAMESPACE}}-token-details-persister"
  AVERAGE_KAFKA_MESSAGE_SIZE_IN_BYTES: "{{AUDIT_AVERAGE_KAFKA_MESSAGE_SIZE_IN_BYTES}}"
  MESSAGES_TO_READ_PER_BATCH: "{{AUDIT_MESSAGES_TO_READ_PER_BATCH}}"
  MAX_WAIT_KAFKA_BATCH_MILLIS: "{{AUDIT_MAX_WAIT_KAFKA_BATCH_MILLIS}}"
  S3_BUCKET: "{{TOKENS_JOB_BUCKET}}"