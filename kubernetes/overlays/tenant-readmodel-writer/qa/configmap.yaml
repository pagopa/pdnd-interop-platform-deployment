apiVersion: v1
kind: ConfigMap
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
data:
  KAFKA_CLIENT_ID: "{{NAMESPACE}}-pagopa-interop-tenant"
  KAFKA_GROUP_ID: "{{NAMESPACE}}-tenant-readmodel-writer"
