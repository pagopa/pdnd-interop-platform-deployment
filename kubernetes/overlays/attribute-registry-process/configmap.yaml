apiVersion: v1
kind: ConfigMap
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
data:
  ACCEPTED_AUDIENCES: "{{UI_JWT_AUDIENCE}},{{M2M_JWT_AUDIENCE}},{{INTERNAL_JWT_AUDIENCE}}"
  ATTRIBUTE_REGISTRY_MANAGEMENT_URL: "{{ATTRIBUTE_REGISTRY_MANAGEMENT_URL}}"
