apiVersion: v1
kind: Service
metadata:
  labels:
    prometheus: "true"
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: spid-login
  selector:
    app: {{SERVICE_NAME}}