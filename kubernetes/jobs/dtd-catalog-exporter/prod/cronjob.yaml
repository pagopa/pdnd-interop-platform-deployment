apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app: {{SERVICE_NAME}}
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
spec:
  schedule: "0 */3 * * *"
  failedJobsHistoryLimit: 1
  successfulJobsHistoryLimit: 0
  jobTemplate:
    metadata:
      labels:
        app: {{SERVICE_NAME}}
      annotations:
        digest: {{IMAGE_DIGEST}} # Used to force deployment on same image version but different content
    spec:
      template:
        metadata:
          labels:
            app: {{SERVICE_NAME}}
        spec:
          restartPolicy: OnFailure
          serviceAccountName: {{SERVICE_NAME}}
          containers:
            - name: {{SERVICE_NAME}}
              image: {{REPOSITORY}}/{{SERVICE_NAME}}:{{JOB_DTD_CATALOG_EXPORTER_IMAGE_VERSION}}
              imagePullPolicy: Always
              env:
                - name: NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: LOG_LEVEL
                  value: info
                - name: READMODEL_DB_HOST
                  value: "{{READ_MODEL_DB_HOST}}"
                - name: READMODEL_DB_PORT
                  value: "{{READ_MODEL_DB_PORT}}"
                - name: READMODEL_DB_NAME
                  value: "{{READ_MODEL_DB_NAME}}"
                - name: ENVIRONMENT
                  value: "{{LOWERCASE_ENV}}"
                - name: TENANTS_COLLECTION_NAME
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: TENANTS_COLLECTION_NAME
                - name: ATTRIBUTES_COLLECTION_NAME
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: ATTRIBUTES_COLLECTION_NAME
                - name: ESERVICES_COLLECTION_NAME
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: ESERVICES_COLLECTION_NAME
                - name: S3_BUCKET
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: S3_BUCKET
                - name: DTD_CATALOG_STORAGE_PATH
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: DTD_CATALOG_STORAGE_PATH
                - name: DTD_CATALOG_CSV_FILENAME
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: DTD_CATALOG_CSV_FILENAME
                - name: DTD_CATALOG_JSON_FILENAME
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: DTD_CATALOG_JSON_FILENAME
                - name: DTD_TENANTS_CSV_FILENAME
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: DTD_TENANTS_CSV_FILENAME
                - name: DTD_TENANTS_JSON_FILENAME
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: DTD_TENANTS_JSON_FILENAME
                - name: GITHUB_REPO
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: GITHUB_REPO
                - name: GITHUB_REPO_OWNER
                  valueFrom:
                      configMapKeyRef:
                        name: {{SERVICE_NAME}}
                        key: GITHUB_REPO_OWNER     
                - name: GITHUB_ACCESS_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: metrics-reports
                      key: BOT_DTD_OPENDATA_GITHUB_ACCESS_TOKEN 
                - name: READMODEL_DB_USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: documentdb
                      key: READONLY_USR
                - name: READMODEL_DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: documentdb
                      key: READONLY_PSW
              resources:
                requests:
                  cpu: "{{SERVICE_RESOURCE_CPU}}"
                  memory: "{{SERVICE_RESOURCE_MEM}}"
                limits:
                  cpu: "{{SERVICE_RESOURCE_CPU}}"
                  memory: "{{SERVICE_RESOURCE_MEM}}"